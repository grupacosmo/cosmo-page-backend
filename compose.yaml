services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=cosmo_backend'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
